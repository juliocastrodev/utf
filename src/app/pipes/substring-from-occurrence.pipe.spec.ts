import { SubstringFromOccurrencePipe } from './substring-from-occurrence.pipe';

describe('SubstringFromOccurrencePipe', () => {
  it('create an instance', () => {
    const pipe = new SubstringFromOccurrencePipe();
    expect(pipe).toBeTruthy();
  });
});
